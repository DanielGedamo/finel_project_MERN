import { box } from "@mui/materiel";
import { styled } from "@mui/system";

const flexBetween = styled(box)({
    display:"flex",
    justifyContent:'space-between',
    alignItems:"center",
})
export default flexBetween;